# v18 基準版最小實作（WP-F）

2026-09-10。範圍＝設計稿 r2 §一／§六＋Codex 二讀第 2 點的邊界：**只做 prompt／schema／渲染／驗證相容調整，不做自動補查、不做 undecidable 旗標、不動 Stage 0 與 gate**。未跑模型、未跑 DD、未 push。

## 一、六問落在 judgment 哪些欄位

| 問 | 判斷句（權威欄） | 必交數字 | 白話 | 條件式區塊 |
|---|---|---|---|---|
| 一 怎麼賺錢 | `reasoning.industry`＋`industry.clock_phase` | 分部占比、最新一季毛利率與營益率 | `plain.six.how_it_makes_money` | `industry.tam_table` |
| 二 競爭優勢 | `moat.trend`＋`trend_evidence` | ROIC spread 或最大客戶份額方向（12 個月內 sourced） | `plain.six.moat` | `moat.competitors`（降選填） |
| 三 成長 | `growth.runway_post_y5` | 三年共識 EPS CAGR（家數／來源）、`endo_ceiling`、缺口歸因 | `plain.six.growth` | `growth.segments` |
| 四 現金與資本配置 | `governance.capalloc_grade` | FCF/NI、SBC/營收、現金去向、債務到期與利率 | `plain.six.capital` | `governance.capital_returns` |
| 五 估值 | `valuation.val_light`＋`val_light_derivation` | `percentile_5y`／`peg`／兩個 upside | `plain.six.valuation` | `valuation.peers` |
| 六 可能看錯在哪 | `premortem.blind_spots[]` 三視角 | `max_dd.lo`／`hi`＋`path_risk` | `plain.six.how_wrong` | — |

`reasoning` 必填集合＝這六題（industry／moat／growth／governance／valuation／premortem），其餘模組承重才寫。

## 二、規則檔刪了什麼（45,359 → 41,289 bytes，−9.0%）

- 估值燈四色與三個盲點救援、品質分體質 veto、final_signal 六步、`long_term_confidence` 映射——逐條重複於 always-on 的 `timing-appendix.md`，改指標＋三件跨節接線。
- §5.R 四象限與四檢查點判準——重複於 `roic-durability.md`，只留兩條公式與兩條接線。
- 5×5「tripwire × 四 archetype」換尺表——重複於 `archetype-gatesets.md`／`cyclical-lens.md`。
- Munger 三維快速評級——無下游、與品質分重複。
- §4.G 商業模式解剖三件套固定交表——併入問一、爭點時才展開。
- §3 單位經濟 mix-shift 必填一行＋「與 §6.B③ 互驗、差 >100bp 兩處重算」——同件事兩節各算一次。
- §6 成長品質 7 問逐條作答——只留「標最弱的一題」。
- §6.C 成長護城河連動三選一——與 `moat.trend` 重複。
- §6.E 成長熄火壓測——移進情境樹當 Bear PE 錨，不在問三另算。
- §7 財務品質三年趨勢／DuPont／CCC／債務到期固定交表、獲利品質四警訊逐項——併入問四＋條件式。
- §6.H 客戶集中度逐欄——與問一議價權重複；集中度只留問一，問三只留留存經濟。
- QC-47「永不修剪清單」（QC 元清單）——流程劇場，對判斷輸出零作用。
- §17 QC-53 獨立一節——降為條件載入表一列。
- §18 QC-13 獨立一節——併入問六（自我攻擊寫進反證紀錄，不另開清單）。

合併：原 20 節重排為 §0.5 共用約定／§1 archetype／§2 六問／§3 情境樹／§4 矛盾與前份對帳／§5 決策層／§6 證據面接線／§7 推導與驗收。

**保留**（逐項確認未動）：§0 北極星、J1 負向證據處置、數字引用優先序四條、`decision_inputs` 22 欄與矩陣／row 8a／8b、三視角反證、cause 分組、ROIIC 唯一推導處、四個條件式區塊觸發條件與未展開標記、定性承重模組（治理可信度／護城河機制／替代威脅）要有證據到結論的理由、Max DD lo／hi 與「推導依據寫在 `reasoning.premortem`」、全部硬接線（Hard／Soft Veto、閘 A／B、40% 估值依賴型、內生天花板 sanity、Bear 地板、破壞性競爭上限 7 分、OI 池 −5pp、capalloc C 三連動、QC-21／27／30／35／49／51／52）。

## 三、schema required 184 → 137（只放寬不收緊）

降選填：`archetype` −3｜`thesis` −1（holding_period）｜`industry` −2（bargaining／profit_pool_dir）｜`moat` −5（execution／pricing／combined／spread_table／competitors）｜`competitors[]` −8｜`roic_durability` −2（quadrant／formula_note）｜`growth` −4（runway_years／seven_questions／decay_signals／trap_rating）｜`quality` −5｜`governance` −2（scorecard／sbc）｜`valuation` −3（tier／fwd_pe／targets）｜`appendix_a` −8（signal／val／ma／verdict／pct_5y／兩 upside／moat_score）｜`reasoning` −4。

維持必填：`decision_inputs` 22、`decision_out` 7、`scenario_ref`／`eps_meta`、dd-meta 直讀欄（`decision_out.role`→`dca_role`、`max_dd.lo`／`hi`、`peg`／`percentile_5y`／`val_light`；`ev5y_pct`／`irr_base_pct` 由腳本回填進 `decision_inputs`）、`blind_spots`、`contradictions`、`triggers[]` 九欄、`kill_metrics[]` 三欄、四個條件式區塊 key、`meta`、`catalysts[]`、`thesis.H/R/single_thing`。**沒有加任何必填**——`kill_metrics` 頂層本就不在 root required，加上去是收緊，維持原狀。新增 optional：`plain.six`。

validator 保障未動：`expandable_block_checks`、`premortem_counterevidence_checks`、`drift_checks`、leak／標點、J1／J2／同源 pair。唯一改動＝`j4_plain_checks` 有 `six` 就查六欄、否則查 `five`（都 WARN）。

## 四、渲染相容

`dd_brief.render_five()` 改回傳 `(heading, html, fallback)`：有 `plain.six` → 標題「六個問題」＋六段；否則走 `five` →「五句話」；再退 oneliner。模板 `<h2>五句話</h2>` → `{{LEAD_HEADING}}`，舊檔代回原字串，**14 份 run 重渲染逐 byte 相同**。`build_dd_meta()` 對 signal／val／ma／verdict／pct_5y／兩 upside 改「`appendix_a` 有值就用、None 才從權威欄投影」（`decision_inputs.signal/val/ma`、`valuation.percentile_5y/upside_*`；verdict 退 signal），舊檔兩側同值故輸出不變；`fpe_fy2`／`peg_fy2` 不投影（口徑待定義，無權威側）。

## 五、新形狀 fixture

`scripts/tests/fixtures/judgment_v18_TXN.json`（52,440 bytes，由 `_src/TXN_20260907` 手工改寫）：`plain` 只留 `six`＋business／stories／growth_funding／prior_compare_reason／evidence_quality；`blind_spots` 四條三視角齊備（evidence／assumption／consequence／ruling／watch／refs），failure_story 與 second_failure 已刪；兩個漂移欄併成一條 `cause="方法變動"`＋`prior_field` 陣列；`capital_returns` 改未展開標記；降選填欄位真的拿掉。

驗收：validate 不帶 evidence 0 FAIL／11 WARN、帶 evidence 0 FAIL／10 WARN（J1 與漂移都過）；brief 渲染出六問各一段、三視角各一條、tile 有 EV +44.7%／Base IRR 8.5／Max DD −45%／估值燈 🟡（分位 100% · PEG 1.28），dd-meta 的 signal／val／ma／verdict／pct_5y／兩 upside 全由投影補回。兩支測試各 +4 條。

## 六、bytes 前後

| 項目 | 前 | 後 |
|---|---|---|
| `judgment-rules.md` | 45,359 | 41,289 |
| `judge.md.tmpl` | 10,834 | 7,866 |
| judge bundle（TXN 重組） | 267,930 | 263,962 |
| schema required | 184 | 137 |

bundle 只降 1.5%，因為九成是證據包與逐字稿；v18 要省的是**輸出**，那要等成對驗證才知道。

## 七、刻意沒做

補查分支與 `undecidable` 旗標及其五個消費端接線（另立工作包）；`dd_decision.py`／`dd_scenario.py`／`gate.md.tmpl`／`gate_contract.md`／coverage／Stage 0／`ddreport.py` 一律未動；未回頭改任何既有 judgment 或已上站報告；三份 always-on reference 一字未改。
