# v19 WP-H2-3 複審（Codex，2026-09-11）：409 測試全過，三項缺口補完再跑 FIX

## 三項缺口

1. **規則檢查只比標頭，沒比正文。** 保留版本戳、改產物內容仍 PASS（`scripts/dd_rules.py:162`）。應重新生成預期內容，與整份產物比對。
2. **散文的 fact id 沒有真的查來源。** 引用全換成不存在的 `f_nonexistent_*`、給空事實表，結構驗收仍 PASS（`scripts/validate_report_v19.py`）。應核對 id 確實存在，且屬於判斷檔允許引用的事實。
3. **審核包仍漏原文與段落定位。** 事實值有帶入，`quote` 與 `source.locator` 沒帶（`scripts/dd_bundle.py:800`）。前三季摘要也應保留穩定引用編號及來源定位。

## 對拿不準的裁定

- 192KB 可以接受，先驗品質與實際總成本，不再為縮包刪內容。
- stub 驗收失敗合理，不必為測試把骨架偽裝成完成稿。
- 每題兩條引用先觀察；誤擋時查原因，不因累積兩例自動降門檻。

## 對照

改用 FIX 可以。先確認既有 v18 B 版的證據、逐字稿與規則版本一致；v19 強制完整判斷，不走 delta／reuse。三項補上並加反例測試後，由持有人授權不發布實測；快速版繼續保留。
