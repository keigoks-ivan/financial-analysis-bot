# v19 WP-H1 複審（Codex，2026-09-11）：支持開 H2，兩個漏擋正式切換前補上

隔離副本重跑：313 個測試全過（指揮 session 報 245 是子集、交接檔報 311 是實作時點）。

## 三題裁定

1. **催化劑保留選填**，同意。行事曆事件與交易觸發條件不同，不能全部從 triggers 還原。沿用「有填就保留，缺席才投影」，程式不補影響度。
2. **不以必填欄位數當目標**，同意。H2 應比較整條流程的時間、總成本、修補回合與品質；把事實整理、散文和重試算進去，不能只看 Fable 省多少。
3. **護城河表在 H2 拆分搬移**，同意部分搬：同業數字、期間、口徑、來源搬到 facts；「對手無力價格戰」、優勢持續性和 checkpoints 燈號仍是判斷。`minItems` 改驗必要項目是否有回答；查無、不適用要有理由，不能靠空物件湊數。

## 兩個漏擋（fixture 實際重現）

- 事實檔不存在：仍 0 FAIL，引用核對與部分投影跳過。
- scenario_meta 不存在：仍 0 FAIL，J2 算術檢查跳過（`scripts/validate_judgment.py:501`）。

「J2 已硬擋」目前只涵蓋資料存在、算出矛盾，尚未涵蓋資料缺失、根本沒算。H2 正式流程須要求事實檔可解析且驗證通過、情境結果完整、J2 確實執行，再允許組頁完成。補上這兩項負向測試後，按既定範圍推進。

## 快速版

待新版完整流程驗收通過再停止產出，既有頁面留存。
