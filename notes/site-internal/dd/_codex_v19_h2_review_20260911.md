# v19 WP-H2 複審（Codex，2026-09-11）：支持 H2-3，補完再跑 TSM 對照

隔離副本重跑 382 個測試全過。

## 三題裁定

1. **前三季摘要：暫不同意完全交給事實整理模型取捨。** 實測：機械初稿不保留只出現在摘要裡的反證；刪掉一條 `findings_digest`，事實檢查仍 0 FAIL、0 WARN；整理失敗退回初稿繼續走已有測試固定。第一次 TSM 對照應保留前三季摘要的全部 items／qa_flags（先壓縮排版）；之後有逐項來源覆蓋檢查再移除全文。正式流程遇整理失敗，不能只記錄 fallback 就當完整輸入。
2. **規則：保留單一人工維護來源，v19 版由程式產生。** 用規則編號選出 v19 所需內容，測試檢查來源版本與產物一致。「衝突時以完整檔為準」無法落實，判斷模型看不到完整檔。
3. **70KB：同意改成結構與內容驗收，真跑前完成。** 同步修流程內檢查、散文 prompt、pre-commit。驗收涵蓋六問、可追溯引用、三視角、行動條件、必要表格、數字一致性；空表、佔位文字不能靠標題齊全過關。

## 兩個接線缺口（併入 H2-3）

- `scripts/ddreport.py:3610` 正式組頁呼叫沒傳 `--layout v19`，仍走舊模板；要測正式流程確實產生新版面。
- `scripts/dd_bundle.py:975` 審核包沒附被引用的 facts 內容——有 id 沒值與原文。至少帶入被引用事實及來源、衝突對照。

## TSM 對照設計

同一份證據跑 v18／v19 才能比分工是否漏資料、是否省成本；與 2026-08-08 舊報告比較只列為歷史裁決對帳，不混成同條件實驗。快速版維持到驗收通過後再停產。
