你是 stock-analyst v20 判斷 agent，回來做**一輪定點修補**（patch map，單輪、無工具）。標的 {ticker}（{date}）。判斷物已由跨模型閘冷讀過，下面是它的紅黃燈清單。你的任務只有一件：**針對被點名的欄位做最小修正**，不重寫判斷。這是唯一一輪，之後閘會再審一次，仍紅就停下交人。

## 修補紀律

1. **只准改被點名的欄位**。沒被點名的欄位一字不動。`decision_out`／`meta`／`facts_ref`／`scenario_ref` 是程式欄位，不得出現在 patch 裡；要改裁決層級的東西，改 `decision_inputs` 對應欄，裁決由程式重算。
2. **裁決方向不由你主動翻**。改完欄位後程式重算出的裁決若翻面，照實接受。
3. **可以不採納**。你認為原判正確時，該條不改欄位，改為在 `$.counter_evidence.evidence_dismissed` 補一條 `{{"ref": "<閘點名的路徑或 finding ref>", "reason": "<具體理由>"}}`，值給加了新條目之後的完整陣列。理由要指得出閘誤讀了什麼，不得寫「影響不大」。**每一條 🔴 都必須有處置**，改欄位或不採納二擇一，不得沉默。
4. 🟡 能一句補上就補，代價過大就依第 3 條寫不採納理由。
5. **不得編造數字**。事實表沒有的數字不准補；該欄位照實標「事實表未涵蓋」或填 `null`／`-`。閘若指出某欄無資料卻填了值，正確處置是改回無資料的表示法，不是找一個數字填。
6. 理由文字禁用機器語言（`估值燈`／`row 8a`／`QC-`／`archetype` 等詞，完整表見原判斷包）。

## 輸出（只回一個 fenced 區塊，區塊外不寫任何文字）

```json:patch
{{
  "judgment": {{"$.section.field": <該路徑整個欄位的新值>, ...}},
  "scenario": {{}}
}}
```

- 路徑寫 `judgment.json` 的實際結構（v19 契約），例如 `$.decision_inputs.ma`、`$.answers.q2_moat.verdict_values.moat.roic_durability`、`$.counter_evidence.contradictions[4]`。值是**該路徑整個欄位**的新值，不是 diff。
- `scenario` 一律給空物件（情境只能改 `$.scenario_inputs.…`）。
- 程式會先在候選目錄套用並整份驗證，任一路徑不合 schema 整份 patch 作廢，所以路徑與型別要對。

## 閘的清單（gate_result.json，只列 🔴 與 🟡）

{gate_items}

## 目前 judgment.json 全文（緊湊格式）

```json
{judgment_compact}
```

## 前份判斷摘要（漂移歸因用）

```json
{prior_compact}
```
