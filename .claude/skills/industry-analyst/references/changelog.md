# industry-analyst v2.6 — changelog.md（條件載入 reference）

> 本檔為 SKILL.md 的拆分模組（2026-07-08 v2.6 結構拆分，內容自 v2.5 原文搬移）。必讀時點見 SKILL.md 對應 stub。

## 【版本歷史】

- **v2.5（2026-07-05，趨勢結構化欄位 + 輸出協議 gate）**：解決三個結構性缺口——① 供需裁決 / 投資時鐘 / conviction / 證偽表只活在散文裡，跨 ID 趨勢無法機器排序、證偽表無消費者;② v2.4 三欄（`demand_5y_multiple` / `purity_pct` / `mcap_bucket`）只是 skill 流程必填、validator 不阻斷，實際首發常漏;③ v2.4 首發違反 dual-output 協議（只產一檔、無 `_full.html`、視覺回退 v2.0 紫版）但既有 10 道 gate 沒攔。修法：(a) **id-meta 新增 5 欄** `sd_verdict`（enum shortage/balanced/surplus/split）+ `sd_verdict_detail`（split 必填 ≤160）+ `clock_phase`（enum I/II/III/IV）+ `conviction`（enum high/mid/low）+ `kill_metrics[]`（≥3 條 `{metric, bear_threshold, window}`，§8 證偽表機器化）;(b) **validator 邏輯升級**：`skill_version` ≥ v2.5 時上述四欄 + v2.4 三欄（頂層 `demand_5y_multiple`;`related_tickers[]` 每個 `depth=🔴` 項的 `purity_pct`/`mcap_bucket`，🟡🟢 選填）全升為阻斷式必填;v2.0–v2.4 present 才驗、absent 放行;v1.x legacy 全豁免。(c) **§0 6-box 三值 + §8 證偽表同步寫 id-meta**;position-thesis-monitor 改直接掃 `kill_metrics`（不再依賴 DD 存在）。(d) **§9 純度% 每檔必附 segment 營收推導**（防「填了但沒驗」;實例＝ASMPT purity 85% 但近半營收是 SMT 非半導體後段）。(e) **Pre-Publish 新增 3 道 gate**：Gate 11（阻斷）dual-output 完整性、Gate 12（阻斷）kill_metrics 同步、Gate 13（warning）purity 推導 → 共 13 道。至此 v2.4 獵場篩選條件 `demand_5y_multiple ≥ 2 ∧ sd_verdict==shortage ∧ conviction ≥ mid` 欄位齊備、可直接機器執行。
- **v2.4（2026-07-02，爆發獵場欄位;長抱數倍股 mandate 校準）**：全鏈 audit 發現 ID 層兩個結構性缺口——① conviction pill 量 thesis 穩固度、不量 upside 量級（5Y 需求 1.3x 的穩產業也能 high conviction），「哪些 ID 是多倍股獵場」在 85 份 ID 間無法排序;② §9 🔴 tier 用「營收依賴>40% OR 技術領導者」把 mega 龍頭與 small cap pure-play 混在同層，但多倍股幾乎只出在「高純度 × 非 mega」組合（149 檔 🔴 無 DD 名單是一團無法排序的名字）。修法：(a) §4 TAM 表末必附 5Y 需求倍數（base 5Y TAM ÷ 現 TAM）→ §0 TL;DR TAM 格 → id-meta 選填 `demand_5y_multiple`;(b) §9 表加「純度%」「市值級距」兩欄 → `related_tickers[]` 每項選填 `purity_pct`/`mcap_bucket`（mega >$200B / large $10-200B / mid $2-10B / small <$2B）;(c) §9 新增強制「營運槓桿最大者」指認（產業爆發時 P&L 彈性最大者，往往非市佔最大者;「非顯而易見受益者」的孿生問題）;(d) conviction 與爆發等級明文正交。三個 id-meta 欄 validator 只在 present 時驗（legacy 全豁免），v2.4+ 新報告必填。下游用法＝多倍候選機器初篩，為 discovery→screener 管線鋪資料地基。
- **v2.3（2026-06-20，dual-output 雙軌 + 精煉版編輯風 + canonical=精煉版）**：① **dual-output** — 每跑一次同產兩份：**canonical `ID_{Theme}_{date}.html` = 精煉版決策卡**（帶 id-meta，索引/screener 認這份）+ `_full.html` = 完整考證版（9 章，無 id-meta、companion）。canonical 翻成精煉版（一點進來就是決策卡），完整版搬 `_full.html`，雙向連結。新 `templates/lean_template.md`。② **精煉版 6 PARTS**（PART I–VI＝決策摘要 / 歷史類比 / 供給 / 需求 / 裁決三情境 / 個股 tier；**砍掉舊草稿的第 7 節「System Integration / Pure MA」** — 賣出訊號併入 PART I monitor）；「決策層完整、證據層濃縮」。③ **個股只輸特徵非推薦**：PART VI + epilogue 必含「特徵/分類、買賣由個股 DD + 系統決定、非買入推薦」。④ **視覺改精煉版編輯風**（取代 v2.2 海軍藍 IB）：暖紙 `#faf7f2` + 墨黑 `#1c1916` + 鏽金 `#7a3d1c`/`#b08840` + 功能語意色；Noto Serif TC/Crimson Pro 襯線 body + IBM Plex Mono 標籤（引 Google Fonts）；元件：masthead / stat-grid / abstract / toc / dsum / keycall / inference（含 deduction-chain + 可證偽）/ scenario-set（主觀機率）/ timeline / monitor / tier-list / keystone / epilogue / footnotes / colophon。**完整版與精煉版同皮**。⑤ Pilot：AIComputeCapexCycle 已套全套（canonical=精煉版 + `_full.html`）。
- **v2.2（2026-06-20，§0 三句話 + 外資券商視覺）**：① **§0 新增「三句話看完」**（INVESTMENT SUMMARY：現在 / 未來 / 行動，§0 第一個內容塊，10 秒答案；**行動允許「都不買 / 等回調」**破除結構性偏多）；原「結論先行段」reframe 為「KEY CALL 最重要的一個判斷」（只放三句話濃縮不下的深層非共識洞見）。② **id-meta 新增 `now_state` / `future_state` / `action` 三欄**，僅 `skill_version` v2.x 必填（`validate_id_meta.py` 阻斷、各 ≤240 chars），legacy v1.x 完全豁免。③ **HTML 改外資券商風格（Classic IB）**：紫色主視覺 → 海軍藍 `#1F2A44` + teal `#0F766E` + 功能語意色（綠/琥珀/酒紅/鋼藍）；中文 body 無襯線、h1/h2/h3+數字卡襯線；masthead stance band（Sector stance + 裁決 + Conviction + Top picks，等同個股 rating+TP）；每張表自動 `Exhibit N.`（CSS counter）；navy 表頭 + 斑馬列；頁尾 Disclosures；去裝飾 emoji（⚡📌📐💡，保留 🔴🟡🟢 tier 語意燈）。**反教條**：顏色要帶語意但不可單一 navy 到底（會單調費眼，pilot 反饋）。④ Pilot：AIComputeCapexCycle 已套全套 v2.2（§0 三句話 + meta + IB 視覺）。
- **v2.1（2026-06-11，pilot 反饋當日修訂）**：用戶對首發 pilot（AIComputeCapexCycle）的兩條反饋落規格：① **深度上修** — 全文 target 8-12K → **16,000-22,000 可見字**（<14K 視為偷懶），各章字數約 ×2，新增「深度擴展規則」八槓桿（機制敘事 / 類比完整敘事 / 資金流圖譜 / 🔴 個體敘事 / kill 完整 steel-man / bear 情境敘事化 / 二階效應 / 編年史段落），擴展靠文字不靠表格；② **結論先行** — §0 第一個內容塊強制 3-6 句純白話結論段（裁決 + 核心非共識 + 真風險 + 行動），禁止用卡片/pill 替代、禁止埋伏筆。另：QC-6 T1 60% 對 macro/aggregate 主題的例外條款待補（pilot 學習，見 memory）。
- **v2.0（2026-06-11）**：**合併 industry-ds（DS）→ 單一「敘事為骨、表格為窗」產業深度報告**。① 全新章節骨架：§0 決策層 + 三幕共 9 章（§0 決策摘要 / §1 白話定義+歷史 / §2 技術 S 曲線 / §3 供給+利潤池 / §4 需求+三角驗證 / §5 供需裁決+推估+投資時鐘 / §6 估值傳導 / §7 Non-Consensus+priced-in+kill / §8 catalyst+證偽 / §9 ticker）— 取代舊 14 章 ID schema 與 DS 11 章 schema。② 吸收 DS 全部：因果敘事弧、§1 雙錨點（DS-9）、因果閉合（DS-2）、推導鏈、§末 aside 來源系統、敘事/條列 hybrid、每章 💡、可讀性規則層（文字 ≥55%、lede、每表三句話、術語白話）。③ 新增 7 個分析模組：歷史 cycle 統計（§1）、利潤池遷移（§3）、成本曲線（§3）、需求三角驗證（§4）、資本週期證據（§5）、庫存訂單指標（§5）、priced-in 檢驗（§7）。④ 研究流程改四軸（歷史 / 供給 / 需求 / 驗證），每軸 3-5 輪。⑤ QC 重編號 QC-1~20 + QC-M1~M6（同構合併：證偽 / 雙路徑 / non-consensus / steel-man 各一條；砍只服務舊結構的 QC-I15 各章最低表格數；新增 6 條對應新模組）。⑥ Pre-Publish 10 gates（保留 ID Gate 1/2/2.1/3 + id-meta validate；併入 DS 文字比 / aside 來源 / 推導 regex / §1 錨點）。⑦ id-meta schema **零改動**（skill_version "v2.0" 為新格式標記；sections_refreshed 沿用三桶，technical→§1-2 / market→§3-5 / judgment→§6-9）。⑧ Step 8.7 critic 改呼叫 id-review（v2 由 skill_version 自動判別）。⑨ 吸收 DS 觸發語（「{主題} ds」「敘述報告」「供需循環」「discourse」）。⑩ HTML 保留紫色主視覺 + S 曲線強制；💡 / PM 綠卡用 DS 綠；value chain SVG 改利潤池語境。既有 105 ID + 8 DS 凍結為 legacy，不 retrofit、不遷移；industry-ds deprecated 觸發語自動轉向。
- **v1.13（2026-05-03）**：frontmatter 版本與日期更新（body 規則同 v1.12）。
- **v1.12（2026-05-02，post-HBM4 validation）**：revert v1.11 evidence pool hooks。同日跑「2026-2027 HBM4 供需循環」ID 端到端驗證，發現自動 fetcher 拿到的（EDGAR press release ≈ WebFetch 已能讀；arXiv paper 多為 PIM tangential；IR archive 1994-2020 噪音 ~800 個）跟 ID 真正需要的（SK Hynix / Samsung Korean IR primary、付費 SemiAnalysis / Yole 報告、投資人日 deck PDF、法說會 Q&A 音訊）結構性錯位。本次 revert 移除 Step 0 / Step 2/3/6 的 evidence read hook、引用規範裡 `[evidence:]` tag 段落 — 回到純 WebSearch / WebFetch 流程。保留：`scripts/evidence/` dead code、`SEMI_EVIDENCE_SPEC.md`（標 DEPRECATED）、`get_whisper_model.sh`。Lesson：別把「自動化好玩」誤當成「自動化有用」。
- **v1.11（2026-05-02，已 revert，見 v1.12）**：新增 Step 0 — Evidence Prefetch（呼叫 `scripts/evidence/orchestrator.py`）+ Step 2/3/6 evidence-read hook + `[evidence:]` citation tag。源於 `SEMI_EVIDENCE_SPEC.md` 設計：補強 ID primary-source 比例。同日驗證失敗，v1.12 移除。
- **v1.10（2026-05-01）**：Claim Taxonomy v1（4-class [F:]/[I:]/[X:]/[A:]）+ 詞彙級機率 + tier-aware enforcement。新增 QC-I24（taxonomy 標記強制）、QC-I25（spurious specificity 禁令）、QC-I26（多情境強制：base/bull/bear，bear 必須對應 §13 真 trigger 不是 strawman）。v1.0-v1.9 共存策略：機會主義升級（不主動 batch retrofit 80 份）。v1.10 ID 必加 reader banner。
- **v1.0（2026-04-19）**：初版。11 章 schema；🟡 規則；QC-I1-I6；stock-analyst 整合鉤子。

> **註**：v1.1-v1.9 的增量規則（六大原則強化、事件型/結構型分類、Gate 2.1 cornerstone、§0.7 PM Implication、id-meta JSON、差別 stale、Reference Card 等）已逐步併入 v1.10-v1.13 主文並由 v2.0 繼承重編號。v2.0 之前歷史完整快照見 git log。

- **v2.6（2026-07-08）priced-in 機器化＋結構拆分＋規則治理**：
  - **`priced_in` 落欄**（low/mid/high，v2.6+ validator 阻斷；ENUM＋必填閘見 scripts/validate_id_meta.py）——§7 各分歧 priced-in 檢驗收斂為整體讀數。WHY：ID 層裁決校準（knowledge/calibration_id_20260707.md）：77 份可結算 ID 全部的錯集中 shortage×Phase II（AI 硬體，勝率 7/25）——sd_verdict 量物理供需非可投資性，priced-in 散文有、機器讀不到；同週 DD 層對 MU/SNDK 判對，證明缺的軸是 priced-in/循環位置。獵場篩選鍵加第四條件 `priced_in ≠ high`。
  - **結構拆分**（stock-analyst v14.7 同款）：id-meta schema 全文（9k）與版本歷史（6k）移 references/ 條件載入，核心 62k→~48k 字元；Gate 13' 載入閘。語意零變更。
  - **規則治理**：conviction pill 公式與 QC-6 T1 60% floor 登記 knowledge/rule_ledger.md（kill condition 制，見 repo CLAUDE.md 治理條款）。
  - Legacy 豁免：v2.5- 報告 priced_in present 才驗、absent 放行；79 份 v2.5 backfill 檔不需回補（下次自然到期重審時補）。

